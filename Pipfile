[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
# Add development dependencies here, if needed
# Example:
# pytest = "*"

[packages]
# Core Apache Airflow installation
apache-airflow = "==2.7.2"

# Database connector for PostgreSQL
psycopg2-binary = "*"

# Celery for task queuing (if using CeleryExecutor)
celery = "*"

# Additional dependencies for commonly used Airflow features
# Amazon S3 and AWS services
apache-airflow[amazon] = "*"
# Google Cloud Platform services
apache-airflow[gcp] = "*"
# Kubernetes (optional if you're deploying Airflow on k8s)
apache-airflow[kubernetes] = "*"
# Slack integration (optional)
apache-airflow[slack] = "*"

# Example additional Python libraries your DAGs may require
requests = "*"
pandas = "*"
numpy = "*"

[requires]
python_version = "3.10"
