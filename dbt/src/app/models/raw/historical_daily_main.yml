version: 2

models:
  - name: historical_daily_main
    alias: historical_daily_main
    description: historical_daily_main table built with dbt
    docs:
      show: true
    config:
      schema: "raw"